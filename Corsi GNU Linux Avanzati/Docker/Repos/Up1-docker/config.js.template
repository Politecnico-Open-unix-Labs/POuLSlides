// Empty if the webapp is in the same place as the server
upload.config.server = '{{SERVER}}'
// upload.config.server = 'https://yourserver.com/' 

// If the webapp is separated from the server - remember the trailing slash
upload.config.api_key = '{{API_KEY}}' 

// Should be the same as the server, used for uploading
upload.config.footer = '{{FOOTER}}'

